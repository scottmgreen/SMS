@page "/Listings/Investigations"
@using SMS3.Components.Shared
@using SMS_Domain.Entities
@using SMS_Domain.ValueObjects
@using SMS_Application.Messaging.Queries
@using SMS_Application.Interfaces
@using SMS_Shared.Common
@using Radzen
@using Radzen.Blazor

<PageTitle>Investigations</PageTitle>
<!-- SMS Page Header using reusable component -->
<PageHeader Title="Investigation Listing"
            Subtitle="Complete listing and management interface for all SMS investigations"
            Icon="fas fa-list" />

<RadzenDataGrid @ref="investigationsGrid" 
                Data="@investigations" 
                Count="@totalCount" 
                LoadData="@LoadData"
                AllowPaging="true" 
                AllowSorting="true" 
                AllowFiltering="true"
                PageSize="25"
                PagerHorizontalAlign="HorizontalAlign.Left"
                ShowPagingSummary="true"
                FilterMode="FilterMode.Advanced"
                SelectionMode="DataGridSelectionMode.Single"
                Style="height: calc(100vh - 200px)"
                IsLoading="@isLoading">
    
    <Columns>
        <RadzenDataGridColumn TItem="Investigation" Property="ReportCode" Title="Report ID" Width="120px">
            <Template Context="investigation">
                <div title="@investigation.ReportCode" style="font-size:smaller;font-weight: 600">
                    @(investigation.ReportCode)
                </div>
            </Template>
        </RadzenDataGridColumn>
        <RadzenDataGridColumn TItem="Investigation" Property="HazardCode" Title="Hazard ID" Width="120px">
            <Template Context="investigation">
                <div title="@investigation.HazardCode" style="font-size:smaller;font-weight: 600">
                    @(investigation.HazardCode)
                </div>
            </Template>
        </RadzenDataGridColumn>
        <RadzenDataGridColumn TItem="Investigation" Property="Code" Title="Investigation ID" Width="150px">
            <Template Context="investigation">
                <div title="@investigation.Code" style="font-size:smaller;font-weight: 600">
                    @(investigation.Code)
                </div>
            </Template>
        </RadzenDataGridColumn>
        <RadzenDataGridColumn TItem="Investigation" Property="Status" Title="Status" Width="225px">
            <Template Context="investigation">
                <div title="@investigation.Status.ToString()" style="font-size:smaller;font-weight: 600">
                    @(investigation.Status.Value)
                </div>
            </Template>
        </RadzenDataGridColumn>
        <RadzenDataGridColumn TItem="Investigation" Property="AssignedInvestigatorId" Title="Assigned To" Width="150px" />
        <RadzenDataGridColumn TItem="Investigation" Property="CompletedDate" Title="Completed Date" Width="150px" FormatString="{0:MM/dd/yyyy}" />
        <RadzenDataGridColumn TItem="Investigation" Property="CreatedBy" Title="Created By" Width="150px" />
        <RadzenDataGridColumn TItem="Investigation" Property="CreatedDate" Title="Created Date" Width="150px" FormatString="{0:MM/dd/yyyy}" />
        
        <RadzenDataGridColumn TItem="Investigation" Title="Actions" Width="150px" Sortable="false" Filterable="false">
            <Template Context="investigation">
                <RadzenStack Orientation="Orientation.Horizontal" Gap="0.5rem">
                    <RadzenButton Icon="visibility" ButtonStyle="ButtonStyle.Base" Size="ButtonSize.Small" 
                                Click="@(() => ViewInvestigation(investigation))" 
                                Tooltip="View Investigation Details" />
                    <RadzenButton Icon="edit" ButtonStyle="ButtonStyle.Primary" Size="ButtonSize.Small" 
                                Click="@(() => EditInvestigation(investigation))" 
                                Tooltip="Edit Investigation" />
                </RadzenStack>
            </Template>
        </RadzenDataGridColumn>
    </Columns>
</RadzenDataGrid>