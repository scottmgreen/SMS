@page "/SMSListings/RiskAnalysis"

@using SMS3.Components.Pages.SMSListings.Models
@using Radzen
@using Radzen.Blazor
@using SMS3.Components.Shared
@using SMS_Application.Interfaces
@using SMS_Application.Queries
@using SMS_Domain.Entities
@using SMS_Domain.ValueObjects
@using SMS_Shared.Common

<PageHeader Title="Risk Analysis"
           Subtitle="Complete listing and management interface for all SMS Risk Analysis"
           Icon="fas fa-list" />

<RadzenDataGrid @ref="_analysisGrid" 
                Data="@_analysisResults" 
                Count="@_totalCount" 
                AllowPaging="true" 
                AllowSorting="true" 
                AllowFiltering="true"
                PageSize="15"
                PagerHorizontalAlign="HorizontalAlign.Left"
                ShowPagingSummary="true"
                FilterMode="FilterMode.Advanced"
                SelectionMode="DataGridSelectionMode.Multiple"
                Style="height: calc(100vh - 200px)">
    
    <Columns>
        <RadzenDataGridColumn TItem="RiskAnalysis" Width="60px" Sortable="false" Filterable="false">
            <HeaderTemplate>
                <input type="checkbox" />
            </HeaderTemplate>
            <Template Context="analysis">
                <input type="checkbox" @onclick:stopPropagation="true" />
            </Template>
        </RadzenDataGridColumn>
        <RadzenDataGridColumn TItem="RiskAnalysis" Property="Code" Title="Risk Analysis ID" Width="150px" />
        <RadzenDataGridColumn TItem="RiskAnalysis" Property="HazardCode" Title="Hazard Code" Width="120px" />
        <RadzenDataGridColumn TItem="RiskAnalysis" Property="RiskAssessmentCode" Title="Risk Assessment ID" Width="150px" />
        <RadzenDataGridColumn TItem="RiskAnalysis" Property="InitialWorstCredibleOutcome" Title="Initial WCO" Width="220px" />
        <RadzenDataGridColumn TItem="RiskAnalysis" Property="InitialRootCause" Title="Initial RC" Width="220px" />
        <RadzenDataGridColumn TItem="RiskAnalysis" Property="ResidualWorstCredibleOutcome" Title="Residual WCO" Width="220px" />
        <RadzenDataGridColumn TItem="RiskAnalysis" Property="ResidualRootCause" Title="Residual RC" Width="220px" />
        <RadzenDataGridColumn TItem="RiskAnalysis" Title="Actions" Width="100px" Sortable="false" Filterable="false">
            <Template Context="analysis">
                <RadzenButton Icon="more_vert" ButtonStyle="ButtonStyle.Light" Size="ButtonSize.Small" 
                            Click="@(() => ShowActions(analysis))" Disabled="true" />
            </Template>
        </RadzenDataGridColumn>
    </Columns>
</RadzenDataGrid>
