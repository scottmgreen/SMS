@inherits LayoutComponentBase
@using SMS3.Components.Layout
@using SMS_Domain.Entities
@using SMS_Domain.Enums
@inject AuthenticationService AuthService
@inject NavigationManager Navigation

<div class="d-flex flex-column min-vh-100 sms-main-layout">
    <!-- Cascading Authentication State to all child components -->
    <CascadingValue Value="@AuthService.AuthState" Name="AuthState">
        <!-- Horizontal Navigation -->
        <header class="sms-main-header">
            <NavMenu OnLogoutRequested="@HandleLogout" />
        </header>

        <!-- Main Content Area -->
        <main class="flex-grow-1 sms-main-content">
            <div class="container-fluid py-3">
                @Body
            </div>
        </main>
    </CascadingValue>
</div>

<!-- Radzen Dialog Components - Required for dialogs to work -->
<RadzenDialog />
<RadzenNotification />
<RadzenTooltip />
<RadzenContextMenu />

<div id="blazor-error-ui" class="sms-error-ui">
    <div class="sms-error-content">
        <i class="fas fa-exclamation-triangle me-2"></i>
        An unhandled error has occurred.
        <a href="" class="reload sms-error-reload">Reload</a>
        <a class="dismiss sms-error-dismiss">🗙</a>
    </div>
</div>

@code {
    protected override void OnInitialized()
    {
        // Subscribe to auth state changes with proper async handling
        AuthService.OnAuthStateChanged(async () => {
            await InvokeAsync(StateHasChanged);
        });
    }

    /// <summary>
    /// Handle logout - called by NavMenu
    /// </summary>
    public void HandleLogout()
    {
        AuthService.ClearAuthentication();
        
        // Navigate to login after clearing authentication
        Navigation.NavigateTo("/Login", forceLoad: true);
    }

    public void Dispose()
    {
        // Note: We can't easily remove the callback since it's a lambda
        // In a production app, you might want to store the callback reference
        // For now, this is acceptable since MainLayout lives for the app lifetime
    }
}

<style>
/* =============================================================================
   SMS MAIN LAYOUT - SIMPLE AND CLEAN
   ============================================================================= */

.sms-main-layout {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

.sms-main-header {
    position: sticky;
    top: 0;
    z-index: 1030;
    background-color: #212e61 !important;
    color: white !important;
}

.sms-main-content {
    min-height: calc(100vh - 70px);
}

/* Error UI Styling */
.sms-error-ui {
    background: #212e61 !important;
    color: white !important;
    padding: 1rem;
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 9999;
    display: none;
    border-top: 2px solid white !important;
}

.sms-error-content {
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 500;
}

.sms-error-reload {
    color: white !important;
    text-decoration: underline;
    margin-left: 1rem;
    font-weight: 600;
}

.sms-error-reload:hover {
    color: white !important;
    opacity: 0.8;
}

.sms-error-dismiss {
    margin-left: 1rem;
    background: none;
    border: none;
    color: white !important;
    font-size: 1.2rem;
    cursor: pointer;
    padding: 0 0.5rem;
    border-radius: 0.25rem;
    transition: background-color 0.2s;
}

.sms-error-dismiss:hover {
    background-color: rgba(255, 255, 255, 0.1);
}

/* Ensure consistent spacing */
.container-fluid {
    padding-left: 1.5rem;
    padding-right: 1.5rem;
}

/* =============================================================================
   END SMS MAIN LAYOUT
   ============================================================================= */
</style>
