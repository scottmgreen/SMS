@inherits LayoutComponentBase

<div class="sms-login-layout">
    <!-- SMS Header - Uses shared NavMenu styling -->
    <nav class="navbar navbar-expand-lg sms-nav">
        <div class="container-fluid">
            <a class="navbar-brand sms-brand" href="/">
                <i class="fas fa-plane-departure me-2"></i>
                PDX SMS
            </a>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="sms-login-container">
        @Body
    </div>
</div>

<style>
/* =============================================================================
   LOGIN LAYOUT SPECIFIC STYLING ONLY
   Navigation styling is shared from app.css
   ============================================================================= */

.sms-login-layout {
    min-height: 100vh;
    background: #212e61 !important;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    color: white !important;
}

/* .sms-login-container {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: calc(100vh - 70px);
    padding: 2rem 1rem;
} */
</style>