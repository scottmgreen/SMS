# Background Image Placeholder

This directory should contain the background image file: `background_opening.jpg`

For now, the CSS will gracefully handle the missing background image.

To add a background image:
1. Place your background image file here as `background_opening.jpg`
2. Ensure it's optimized for web use (recommended size: 1920x1080 or similar)
3. The image will automatically be used for the login page and unauthenticated home page

The background image should represent the Port of Portland operations in a professional manner.